name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: aspmenabler-x86_64
          - os: ubuntu-24.04-arm
            artifact_name: aspmenabler-arm64
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile
        run: gcc -O2 -flto -Wall -Wextra aspmenabler.c -o aspmenabler

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: aspmenabler

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/aspmenabler-x86_64/aspmenabler
            artifacts/aspmenabler-arm64/aspmenabler